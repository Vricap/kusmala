// fungsi rekursif untuk generate barisan fibonacci
buat fib = fungsi(a, b, limit) {
	jika(a > limit) {
		kembalikan a;
	} lainnya {
		cetak(a, " ");
		fib(b, a + b, limit);
	}
}

buat num = 8;
fib(0, 1, num);
