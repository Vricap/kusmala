buat a = 1;
buat b = 2;

buat f = fungsi (x, y) { // fungsi literal
	jika (x > y) {
		kembalikan x;
	} lainnya {
		kembalikan y;
	}
}

buat c = f(a, b);
cetak(c); // cetak akan mengeluarkan hasil ke stdout
